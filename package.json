{
  "private": true,
  "dependencies": {
    "websocket": "^1.0.28"
  },
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:server": "spago --no-psa bundle-app --to dist/server.js --main Server",
    "build:client": "spago --no-psa bundle-app --to dist/client.js --main Client"
  }
}

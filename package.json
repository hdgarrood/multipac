{
  "private": true,
  "dependencies": {
    "websocket": "~1.0.10"
  }
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:client": "pulp browserify --to dist/client.js --main Client",
    "build:server": "pulp browserify --to dist/server.js --main Server"
  }
}
